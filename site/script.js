const menuButton=document.querySelector('.menu-toggle');const navigation=document.querySelector('#site-nav');menuButton.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));navigation.classList.toggle('open',!open)});navigation.addEventListener('click',event=>{if(event.target.matches('a')){menuButton.setAttribute('aria-expanded','false');navigation.classList.remove('open')}});

const reducedMotion=window.matchMedia('(prefers-reduced-motion: reduce)');const scrollRail=(rail,direction)=>rail.scrollBy({left:direction*Math.min(rail.clientWidth*.82,720),behavior:reducedMotion.matches?'auto':'smooth'});document.querySelectorAll('[data-rail]').forEach(rail=>{document.querySelector(`[data-rail-prev="${rail.id}"]`)?.addEventListener('click',()=>scrollRail(rail,-1));document.querySelector(`[data-rail-next="${rail.id}"]`)?.addEventListener('click',()=>scrollRail(rail,1));rail.addEventListener('keydown',event=>{if(event.key==='ArrowRight'){event.preventDefault();scrollRail(rail,1)}if(event.key==='ArrowLeft'){event.preventDefault();scrollRail(rail,-1)}})});

const dialog=document.querySelector('.lightbox');const dialogImage=dialog.querySelector('img');let opener;document.querySelectorAll('.tattoo,[data-lightbox]').forEach(button=>button.addEventListener('click',()=>{opener=button;dialogImage.src=button.dataset.src;dialogImage.alt=button.dataset.alt;dialog.showModal()}));const closeDialog=()=>dialog.close();dialog.querySelector('.lightbox-close').addEventListener('click',closeDialog);dialog.addEventListener('click',event=>{if(event.target===dialog)closeDialog()});dialog.addEventListener('close',()=>{dialogImage.removeAttribute('src');opener?.focus()});

document.querySelectorAll('details').forEach(item=>item.addEventListener('toggle',()=>{if(item.open)document.querySelectorAll('details[open]').forEach(other=>{if(other!==item)other.open=false})}));

document.querySelector('#enquiry-form').addEventListener('submit',event=>{event.preventDefault();const data=new FormData(event.currentTarget);const subject=`Tattoo enquiry from ${data.get('name')}`;const body=[`Hi AJ,`,``,`My name is ${data.get('name')}.`,`Email: ${data.get('email')}`,`Phone: ${data.get('phone')}`,``,`Tattoo idea:`,String(data.get('idea')),``,`Looking forward to hearing from you!`].join('\n');window.location.href=`mailto:inky.amyjane@gmail.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`});document.querySelector('#year').textContent=new Date().getFullYear();
