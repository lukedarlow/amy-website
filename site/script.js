const menuButton=document.querySelector('.menu-toggle');const navigation=document.querySelector('#site-nav');menuButton.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));navigation.classList.toggle('open',!open)});navigation.addEventListener('click',event=>{if(event.target.matches('a')){menuButton.setAttribute('aria-expanded','false');navigation.classList.remove('open')}});

const gallery=document.querySelector('[data-gallery]');const galleryStep=()=>Math.min(gallery.clientWidth*.82,720);document.querySelector('[data-gallery-prev]').addEventListener('click',()=>gallery.scrollBy({left:-galleryStep(),behavior:'smooth'}));document.querySelector('[data-gallery-next]').addEventListener('click',()=>gallery.scrollBy({left:galleryStep(),behavior:'smooth'}));gallery.addEventListener('keydown',event=>{if(event.key==='ArrowRight'){event.preventDefault();gallery.scrollBy({left:galleryStep(),behavior:'smooth'})}if(event.key==='ArrowLeft'){event.preventDefault();gallery.scrollBy({left:-galleryStep(),behavior:'smooth'})}});

const dialog=document.querySelector('.lightbox');const dialogImage=dialog.querySelector('img');let opener;document.querySelectorAll('.tattoo').forEach(button=>button.addEventListener('click',()=>{opener=button;dialogImage.src=button.dataset.src;dialogImage.alt=button.dataset.alt;dialog.showModal()}));const closeDialog=()=>dialog.close();dialog.querySelector('.lightbox-close').addEventListener('click',closeDialog);dialog.addEventListener('click',event=>{if(event.target===dialog)closeDialog()});dialog.addEventListener('close',()=>opener?.focus());

document.querySelectorAll('details').forEach(item=>item.addEventListener('toggle',()=>{if(item.open)document.querySelectorAll('details[open]').forEach(other=>{if(other!==item)other.open=false})}));

document.querySelector('#enquiry-form').addEventListener('submit',event=>{event.preventDefault();const data=new FormData(event.currentTarget);const subject=`Tattoo enquiry from ${data.get('name')}`;const body=[`Hi AJ,`,``,`My name is ${data.get('name')}.`,`Email: ${data.get('email')}`,`Phone: ${data.get('phone')||'Not provided'}`,``,`Tattoo idea:`,String(data.get('idea')),``,`Looking forward to hearing from you!`].join('\n');window.location.href=`mailto:inky.amyjane@gmail.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`});document.querySelector('#year').textContent=new Date().getFullYear();
